# Repository Agent Rules

## Git Flow (Required)
- For every code change, create a new branch before editing.
- Never push feature changes directly to `main`.
- Commit only to the feature branch.
- Open a Pull Request from the feature branch to `main`.
- Continue updates on the same feature branch until the PR is approved.
- Merge through the PR (prefer squash merge unless the user asks otherwise).
